<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jet Code - No-Code Solutions at Jet Speed</title>
    <style>
        @font-face {
            font-family: 'PixelFont';
            src: url('https://example.com/path/to/pixel-font.woff2') format('woff2'),
                 url('https://example.com/path/to/pixel-font.woff') format('woff');
        }
        body {
            font-family: 'PixelFont', sans-serif;
            background-color: #ffffff;
            color: #000000;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        header {
            text-align: center;
            margin-bottom: 50px;
        }
        h1 {
            font-size: 3em;
        }
        p {
            font-size: 1.2em;
            margin: 10px 0;
        }
        .button {
            background-color: #000000;
            color: #ffffff;
            border: none;
            padding: 15px 30px;
            font-size: 1em;
            cursor: pointer;
            margin-top: 20px;
            text-transform: uppercase;
        }
        .button:hover {
            background-color: #333333;
        }
    </style>
</head>
<body>
    <header>
        <h1>Jet Code</h1>
        <p>Transform Ideas into Reality at Jet Speed</p>
        <p>Accelerate Your Digital Journey with Custom No-Code Solutions</p>
        <button class="button">Get Started</button>
    </header>
</body>
</html>
